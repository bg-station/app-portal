# GitHub Pages セットアップ手順

このファイルは、GitHub PagesでApp Portalを公開するための手順を説明します。

## 📋 前提条件

- GitHubアカウントを持っていること
- このリポジトリ（bg-station/app-portal）への書き込み権限があること

## 🚀 デプロイ手順

### 1. GitHub Pagesを有効化

1. GitHubでリポジトリページを開く
   ```
   https://github.com/bg-station/app-portal
   ```

2. 画面上部の **Settings**（⚙️設定）タブをクリック

3. 左サイドバーの **Pages** をクリック

4. **Source** セクションで以下を設定：
   - **Source**: `Deploy from a branch` を選択
   - **Branch**: `main` (または `master`) を選択
   - **Folder**: `/ (root)` を選択

5. **Save** ボタンをクリック

### 2. デプロイ完了を確認

1. 数分待つ（初回デプロイには2-5分程度かかります）

2. ページを更新すると、Pagesセクションの上部に以下のようなメッセージが表示されます：
   ```
   ✅ Your site is live at https://bg-station.github.io/app-portal/
   ```

3. リンクをクリックしてサイトを確認

### 3. デプロイ状況の確認

1. リポジトリの **Actions** タブを開く

2. `pages build and deployment` というワークフローの実行状況を確認

3. 緑色のチェックマーク（✅）が表示されれば成功

## 🔄 更新の反映

以降、`main`ブランチに変更をプッシュすると、自動的にサイトが更新されます：

1. ファイルを編集（例: `apps.json`に新しいアプリを追加）
2. 変更をコミット＆プッシュ
3. GitHub Actionsが自動的に実行され、数分でサイトに反映

## 🎨 カスタムドメインの設定（オプション）

独自ドメインを使用する場合：

1. Settings > Pages > Custom domain に独自ドメインを入力

2. DNSプロバイダーで以下のレコードを設定：
   ```
   CNAME: your-domain.com → bg-station.github.io
   ```

3. **Enforce HTTPS** にチェックを入れる（推奨）

詳細: https://docs.github.com/ja/pages/configuring-a-custom-domain-for-your-github-pages-site

## 🐛 トラブルシューティング

### サイトが表示されない

1. **Actionsタブ**でデプロイが成功しているか確認
2. ブラウザのキャッシュをクリア（Ctrl+Shift+R / Cmd+Shift+R）
3. しばらく待つ（最大10分程度）

### 404エラーが表示される

1. Pagesの設定でブランチとフォルダが正しいか確認
2. `index.html`がリポジトリのルートに存在するか確認

### スタイルやJavaScriptが読み込まれない

1. `.nojekyll`ファイルがリポジトリのルートに存在するか確認
2. `index.html`内のパスが相対パス（`./`や`../`なし）になっているか確認

### apps.jsonの変更が反映されない

1. JSONの構文が正しいか確認（Actions > validate-apps.ymlの実行結果を確認）
2. ブラウザのキャッシュをクリア
3. デベロッパーツールのネットワークタブで`apps.json`が更新されているか確認

## 📚 参考リンク

- [GitHub Pages ドキュメント（日本語）](https://docs.github.com/ja/pages)
- [GitHub Pages について](https://docs.github.com/ja/pages/getting-started-with-github-pages/about-github-pages)
- [GitHub Pages のトラブルシューティング](https://docs.github.com/ja/pages/getting-started-with-github-pages/troubleshooting-404-errors-for-github-pages-sites)

## 💡 ヒント

- サイトのURLは `https://<organization>.github.io/<repository>/` の形式になります
- このプロジェクトの場合: `https://bg-station.github.io/app-portal/`
- デプロイは自動化されているため、手動での操作は不要です
- `apps.json`を更新するだけで、簡単に新しいアプリを追加できます
